# Project Scope
This application provides a TUI for searching the web for political members
websites and running WHOIS lookups on the results to see if the domain is eligible for purchase.
# Run Builds
Run .\build.bat to build the application.